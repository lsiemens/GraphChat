# Engine GraphChat
The python code used for the GraphChat API server and derived tools.

The directories are:
- `api_server`: The API server for GraphChat.
- `core`: The core python modules for this project.
- `http_server`: A simple (and insecure) http server.
- `tui`: A TUI chat client derived from the modules in core.
