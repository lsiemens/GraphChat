# HTTP Server

This is a basic HTTP server that will support the GraphChat engine in
communicating with the typescript client. While I intend to get it working with
the client, this server was also written as an aid to learning about how servers
work and so uses minimal libraries and is not primarily concerned with
efficiency.
